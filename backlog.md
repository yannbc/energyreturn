# Backlog

Tracked items deferred for later. Resolve when there's a natural reason to touch the area.

---

## Stale per-company application folders

`applications/prezzee/`, `applications/nine/`, `applications/mastercard/`, `applications/derwent/` pre-date the role-specific slug convention (e.g. `prezzee-head-of-ai`). Kept for now to preserve history; delete when they become confusing.

## Cover letter sign-off inconsistency

Cowork-produced letters (the six current Cowork folders) close with "Kind regards, Yann Burden". The HIPages letter uses "Cheers, Yann" per the updated `claude-cowork-handoff.md`. Standardise once Cowork has emitted a full batch in the new format, so we know the convention has taken.
